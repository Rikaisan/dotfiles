# If you come from bash you might have to change your $PATH.
export PATH=$HOME/bin:$HOME/.local/bin:$HOME/.local/bin/rikai:/usr/local/bin:$PATH

# Aliases
if [[ -e ~/.config/rikai/aliases ]]; then
  source ~/.config/rikai/aliases
fi